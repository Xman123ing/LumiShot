# LumiShot Usage

1. Open LumiShot.
2. Select a capture mode (region/window/full-screen/scrolling).
3. Add annotations (text/box/arrow/number).
4. Extract text from image/web/pdf source.
5. Export image (PNG/JPEG) and text (TXT/Markdown).
